# QuantumGPT v2 — Python Dependencies
# Core
torch>=2.0.0
numpy>=1.24.0
flask>=2.3.0

# Training utilities
tqdm>=4.65.0

# Optional: for AMD GPU (ROCm)
# Install PyTorch with ROCm support from:
# https://pytorch.org/get-started/locally/
# e.g.: pip install torch --index-url https://download.pytorch.org/whl/rocm5.6

# Development
# pytest>=7.0.0
