# Model Configuration Parameters

# Model type (e.g., 'transformer', 'LSTM')
model_type = 'transformer'

# Number of layers in the model
num_layers = 6

# Hidden size of each layer
hidden_size = 512

# Number of attention heads
num_attention_heads = 8

# Feedforward layer size
feedforward_size = 2048

# Dropout rate
dropout_rate = 0.1

# Vocabulary size
vocab_size = 10000

# Maximum sequence length
max_seq_length = 512

# Learning rate
learning_rate = 0.001

# Batch size
batch_size = 32

# Number of training epochs
num_epochs = 10

# Device for computation (e.g., 'cpu', 'cuda')
device = 'cuda'